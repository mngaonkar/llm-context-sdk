apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-context-sdk

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: llm-context-sdk
  version: v1

images:
  - name: llm-context-sdk
    newName: llm-context-sdk
    newTag: latest
