# Setup config DB. TODO: refactor, read from file and configure
CONFIG_FILES_PATH = "./deploy/configuration"

CONFIG_DB_PATH = "./deploy/configdb"
CONFIG_DB_NAME = "config.db"
CONFIG_DB = f"{CONFIG_DB_PATH}/{CONFIG_DB_NAME}"

